import 'package:web_socket_channel/web_socket_channel.dart';

WebSocketChannel createWebSocketChannel(String url) {
  throw UnsupportedError('Cannot create a WebSocketChannel without the necessary imports');
}